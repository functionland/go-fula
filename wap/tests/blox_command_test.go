package test

import (
	"testing"
)

func TestBloxCommand(t *testing.T) {
	/*clientPeerId, err := blox.BloxCommandInitOnly("12D3KooWLZrtbay8AM5mrxPZ8ngWq4aS9UTp7SMKhvZRkLhwgErN")
	if err != nil {
		t.Errorf("running blox command: %v", err)
	}
	if clientPeerId == "" {
		t.Error("empty client peer id")
	}
	t.Logf("client peer id: %s", clientPeerId)*/
}
